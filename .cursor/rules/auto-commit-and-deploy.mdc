---
description: whatsapp-new 改完代码后须同一轮内 git 提交、push，并自动上传（SSH 部署）到生产服务器
globs: "**/*"
alwaysApply: true
---

# 强制：改完即自动上传服务器（whatsapp-new）

适用范围：**本仓库**（QE 渣狗控制台 / `whatsapp-new`）。  
**「自动上传服务器」定义**：助手在完成对本仓库业务代码、Runner、前端、`scripts/`、`deploy/`（非机密）等**有效修改后，必须在同一轮对话收尾时自行执行部署**，不得只改本地、不得让用户手动猜命令、不得省略上线步骤。

**与通用习惯的冲突**：在本仓库内，本规则**优先于**「未明确要求则不要 git 提交」——只要本次任务包含可发布的代码/配置改动，就**必须**提交、推送并部署（用户当轮明确说「不要提交/不要上线」的除外）。

## 标准一键流程（优先）

在仓库根目录（有 `package.json` 处），使用 **Git Bash 或 WSL** 执行：

```bash
bash scripts/deploy-server.sh
```

该脚本会：`git push` 当前分支 → SSH 到服务器 → `git fetch/checkout/reset` → `npm ci` → `npm run build` → `chmod dist` → `systemctl restart qe-runner`。  
环境变量可覆盖：`DEPLOY_SSH`、`DEPLOY_BRANCH`、`DEPLOY_REMOTE_DIR`。

若当前环境**无法运行 bash**，则在本机先 `git push`，再用手动等价 SSH 步骤完成与脚本相同的远端操作（见下），并在回复中说明原因。

## 必须执行（缺一不可）

1. **安全**：禁止将 `deploy/runner.env`、`deploy/auth-users.json`、`deploy/auth-users-for-friend.json`、根目录 `.env` 等机密文件加入版本库；提交前确认 `git status` 中无上述内容（应被 `.gitignore` 忽略）。
2. **检查**：`git status`、`git diff`，只暂存与本次任务相关的文件。
3. **提交**：`git commit`（信息简短说明目的与影响）。
4. **推送**：`git push` 到 **`origin` 上当前工作分支**（脚本默认分支见 `DEPLOY_BRANCH`，常见为 `cursor/qe-system-scaffold-de8c`）。
5. **服务器部署**（与 `deploy-server.sh` 远端逻辑一致）：对 **`root@107.149.189.110`** SSH **端口 `27637`**，在 **`/opt/whatsapp-new`**：
   ```bash
   export PATH=/root/.nvm/versions/node/v20.20.2/bin:$PATH
   cd /opt/whatsapp-new
   git fetch origin
   git checkout <当前发布分支>
   git reset --hard origin/<当前发布分支>
   npm ci
   npm run build
   chmod -R a+rX dist
   systemctl restart qe-runner
   ```
6. **构建后权限**：必须在服务器上对 **`dist`** 执行 **`chmod -R a+rX dist`**，避免 Nginx 读静态文件 **403**。

## 失败时的处理

- **`git push` 失败**（鉴权、网络）：立即告知用户，**不得**谎称已上线。
- **服务器 `git fetch` 失败**：可改为 **tar/scp** 同步关键路径后再在服务器 `npm ci && npm run build` 并 `chmod`、`restart`，须向用户说明采用了备用方式。

## 不在此流程自动覆盖的范围

- 服务器 **`/etc/default/qe-runner`**、**`/etc/nginx/sites-available/qe-app`** 等系统级配置：仅在本轮修改**明确涉及**时再改并说明；日常代码发布**不**自动覆盖。

## 例外（可不部署）

- 仅改 `.cursor/rules`、纯文档、与本项目运行无关且用户**当轮明确**说「不用上线」——若无法判断，**默认仍走完整流程**。
